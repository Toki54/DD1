<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>
			{% block title %}Bienvenue sur Dial And Date !
			{% endblock %}
		</title>

		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
		<link rel="stylesheet" href="{{ asset('css/base.css') }}">
	</head>

	<body
		class="d-flex flex-column min-vh-100 bg-dark text-white">

		{# NAVBAR #}
		<header>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm px-4 py-2">
	<a class="navbar-brand text-gold fw-bold" href="{{ path('app_home') }}">💛 Dial & Date</a>
	<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="mainNavbar">
		<ul class="navbar-nav ms-auto align-items-center">

			{% if app.user %}
<li class="nav-item me-2">
	<a href="{{ path('app_messages') }}" class="btn btn-outline-light position-relative">
		📩 Messages
		{% set unread = unread_messages_count() %}
		{% if unread > 0 %}
			<span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
				{{ unread }}
				<span class="visually-hidden">nouveaux messages</span>
			</span>
		{% endif %}
	</a>
</li>


				<li class="nav-item me-2">
					<a class="nav-link text-white" href="{{ path('app_profile_show') }}">👤 Mon Profil</a>
				</li>
<li class="nav-item me-2">
	<a href="{{ path('app_profile_likes') }}" class="btn btn-outline-primary position-relative">
		👁️❤️ Visites & Likes
		{% if unread_likes_count() > 0 %}
			<span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
				{{ unread_likes_count() }}
			</span>
		{% endif %}
	</a>
</li>

				<li class="nav-item me-2">
					<a class="nav-link text-white" href="{{ path('app_profiles_list') }}">🧑‍🤝‍🧑 Profils</a>
				</li>
				<li class="nav-item me-2">
					<a class="nav-link text-gold" href="{{ path('app_stripe_subscribe') }}">💳 Abonnements</a>
				</li>
				<li class="nav-item me-3">
					<a class="nav-link text-danger" href="{{ path('app_logout') }}">🚪 Déconnexion</a>
				</li>

				{% if 'ROLE_ADMIN' in app.user.roles %}
					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle text-warning" href="#" id="adminDropdown" role="button" data-bs-toggle="dropdown">
							⚙️ Admin
						</a>
						<ul class="dropdown-menu dropdown-menu-dark" aria-labelledby="adminDropdown">
							<li>
								<a class="dropdown-item" href="{{ path('app_user_index') }}">👥 Gérer Utilisateurs</a>
							</li>
							<li>
								<a class="dropdown-item" href="{{ path('app_deleted_conversations') }}">🗑️ Conversations Supprimées</a>
							</li>
							<li>
								<a class="dropdown-item" href="{{ path('admin_subscriptions_index') }}">💼 Gérer Abonnements</a>
							</li>
						</ul>
					</li>
				{% endif %}
			{% endif %}
		</ul>
	</div>
</nav>


		</header>

		

		{# ZONE DE CONTENU #}
		<main class="container my-5 flex-grow-1">
			{% block body %}
				
			{% endblock %}
		</main>

		{# FOOTER #}
		<footer class="bg-black text-white text-center py-3 mt-auto">
			<p>&copy;
				{{ "now"|date("Y") }}
				Dial And Date. Tous droits réservés.</p>
		</footer>

		{# JS Bootstrap #}
		 <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
	</body>
</html>

