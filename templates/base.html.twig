<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>
			{% block title %}Bienvenue sur Dial And Date !
			{% endblock %}
		</title>

		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
		<link rel="stylesheet" href="{{ asset('css/base.css') }}">
	</head>

	<body
		class="d-flex flex-column min-vh-100 bg-dark text-white">

		{# NAVBAR #}
		<header>
			<nav class="navbar navbar-expand-lg navbar-dark bg-black shadow">
				<div class="container-fluid">
					<a class="navbar-brand fw-bold" href="{{ path('app_home') }}">Dial And Date</a>
					<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
						<span class="navbar-toggler-icon"></span>
					</button>
					<div class="collapse navbar-collapse" id="navbarNav">
						<ul class="navbar-nav ms-auto">
							{% if not app.user %}
								<li class="nav-item">
									<a class="nav-link" href="{{ path('app_home') }}">Accueil</a>
								</li>
								<li class="nav-item">
									<a class="nav-link" href="{{ path('app_login') }}">Login</a>
								</li>
								<li class="nav-item">
									<a class="nav-link" href="{{ path('app_register') }}">Inscription</a>
								</li>
							{% else %}
								<li class="nav-item">
									<a href="{{ path('app_messages') }}" class="btn btn-info mb-2 me-2">📩 Messages</a>
								</li>
								<li class="nav-item">
									<a class="nav-link" href="{{ path('app_profile_show') }}">Mon Profil</a>
								</li>
								<li class="nav-item">
									<a href="{{ path('app_profile_likes') }}" class="btn btn-primary me-2">
										👁️❤️ Visites & Likes
									</a>
								</li>
								<li class="nav-item">
									<a class="nav-link" href="{{ path('app_profiles_list') }}">Liste des Profils</a>
								</li>
								<li class="nav-item">
									<a class="nav-link" href="{{ path('app_stripe_subscribe') }}">Abonnements</a>
								</li>
								<li class="nav-item">
									<a class="nav-link text-danger" href="{{ path('app_logout') }}">Logout</a>
								</li>

								{% if 'ROLE_ADMIN' in app.user.roles %}
									<li class="nav-item">
										<a class="nav-link text-warning" href="{{ path('app_user_index') }}">Gérer Utilisateurs</a>
									</li>
									<li class="nav-item">
										<a class="nav-link text-warning" href="{{ path('app_deleted_conversations') }}">Conversations supprimées</a>
									</li>
									<li class="nav-item">
										<a class="nav-link text-warning" href="{{ path('admin_subscriptions_index') }}">Gérer Abonnements</a>
									</li>
								{% endif %}
							{% endif %}
						</ul>
					</div>
				</div>
			</nav>
		</header>

		

		{# ZONE DE CONTENU #}
		<main class="container my-5 flex-grow-1">
			{% block body %}
				
			{% endblock %}
		</main>

		{# FOOTER #}
		<footer class="bg-black text-white text-center py-3 mt-auto">
			<p>&copy;
				{{ "now"|date("Y") }}
				Dial And Date. Tous droits réservés.</p>
		</footer>

		{# JS Bootstrap #}
		 <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
	</body>
</html>

