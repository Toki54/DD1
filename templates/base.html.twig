<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8"/>
		<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
		<title>
			{% block title %}Bienvenue sur Dial And Date !
			{% endblock %}
		</title>

		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
		<link rel="stylesheet" href="{{ asset('css/base.css') }}"> {% block stylesheets %}{% endblock %}
		</head>

		<body class="d-flex flex-column min-vh-100">

			<header>
				<nav class="navbar navbar-expand-lg navbar-dark bg-dark px-4 py-3 rounded-bottom shadow-lg">
					<div class="container-fluid">
						<a class="navbar-brand fw-bold text-accent" href="{{ path('app_home') }}">
							💛 Dial & Date
						</a>

						<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar">
							<span class="navbar-toggler-icon"></span>
						</button>

						<div class="collapse navbar-collapse mt-3 mt-lg-0" id="mainNavbar">
							{% if app.user %}
								<div class="d-flex flex-column flex-lg-row w-100 justify-content-between align-items-lg-center gap-3">
									<div class="compteur">
										👥
										{{ total_users_count() }}
										membres inscrits
									</div>

									<ul class="navbar-nav flex-column flex-lg-row gap-2">
										<li class="nav-item">
											<a href="{{ path('app_messages') }}" class="btn btn-dark btn-sm-custom">📩 Messages</a>
										</li>
										<li class="nav-item">
											<a href="{{ path('app_profile_likes') }}" class="btn btn-dark btn-sm-custom">👁️❤️ Likes</a>
										</li>
										<li class="nav-item">
											<a href="{{ path('app_profile_show') }}" class="btn btn-dark btn-sm-custom">👤 Mon Profil</a>
										</li>
										<li class="nav-item">
											<a href="{{ path('app_profiles_list') }}" class="btn btn-dark btn-sm-custom">🧑‍🤝‍🧑 Profils</a>
										</li>
										<li class="nav-item">
											<a href="{{ path('app_stripe_subscribe') }}" class="btn btn-dark btn-sm-custom text-accent border-accent">💳 Abonnements</a>
										</li>
										<li class="nav-item">
											<a href="{{ path('app_logout') }}" class="btn btn-outline-danger btn-sm-custom">🚪 Déconnexion</a>
										</li>

										{% if 'ROLE_ADMIN' in app.user.roles %}
											<li class="nav-item dropdown">
												<a class="btn btn-dark btn-sm-custom dropdown-toggle text-warning" href="#" id="adminDropdown" role="button" data-bs-toggle="dropdown">
													⚙️ Admin
												</a>
												<ul class="dropdown-menu dropdown-menu-dark">
													<li>
														<a class="dropdown-item" href="{{ path('app_user_index') }}">👥 Gérer Utilisateurs</a>
													</li>
													<li>
														<a class="dropdown-item" href="{{ path('app_deleted_conversations') }}">🗑️ Conversations Supprimées</a>
													</li>
													<li>
														<a class="dropdown-item" href="{{ path('admin_subscriptions_index') }}">💼 Gérer Abonnements</a>
													</li>
												</ul>
											</li>
										{% endif %}
									</ul>
								</div>
							{% endif %}
						</div>
					</div>
				</nav>
			</header>

			<main class="container my-5 flex-grow-1"> {% block body %}{% endblock %}
				</main>

				<footer class="bg-black text-white text-center py-3 mt-auto">
					<p>&copy;
						{{ "now"|date("Y") }}
						Dial And Date. Tous droits réservés.</p>
				</footer>

				 <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
			</body>
		</html>

