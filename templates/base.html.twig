<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>
			{% block title %}Bienvenue sur Dial And Date !
			{% endblock %}
		</title>

		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
		<link rel="stylesheet" href="{{ asset('css/base.css') }}">
	</head>
	<body
		class="d-flex flex-column min-vh-100">

		{# NAVBAR #}
		<header>
			<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
				<div class="container-fluid">
					<a class="navbar-brand" href="{{ path('app_home') }}">Site de Rencontre</a>
					<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
						<span class="navbar-toggler-icon"></span>
					</button>

					<div class="collapse navbar-collapse" id="navbarNav">
						<ul class="navbar-nav ms-auto">
							{% if not app.user %}
								<li class="nav-item">
									<a class="nav-link" href="{{ path('app_home') }}">Accueil</a>
								</li>
								<li class="nav-item">
									<a class="nav-link" href="{{ path('app_login') }}">Login</a>
								</li>
								<li class="nav-item">
									<a class="nav-link" href="{{ path('app_register') }}">Registration</a>
								</li>
							{% else %}
								{# Liens utilisateur normal #}
								<li class="nav-item">
									<a href="{{ path('app_messages') }}" class="btn btn-info mb-3">Voir mes messages</a>
								</li>
								<li class="nav-item">
									<a class="nav-link" href="{{ path('app_profile_show') }}">Mon Profil</a>
								</li>
								<li class="nav-item">
									<a class="nav-link" href="{{ path('app_profile_visits') }}">
										👁️ Visites reçues
									</a>
								</li>

								<li class="nav-item">
									<a class="nav-link" href="{{ path('app_profiles_list') }}">Liste des Profils</a>
								</li>
								<li class="nav-item">
									<a class="nav-link" href="{{ path('app_stripe_subscribe') }}">Abonnements</a>
								</li>
								<li class="nav-item">
									<a class="nav-link text-danger" href="{{ path('app_logout') }}">Logout</a>
								</li>

								{# Si l'utilisateur est un admin #}
								{% if 'ROLE_ADMIN' in app.user.roles %}
									<li class="nav-item">
										<a class="nav-link text-warning" href="{{ path('app_user_index') }}">Gérer Utilisateurs</a>
									</li>
									<li class="nav-item">
										<a class="nav-link text-warning" href="{{ path('app_deleted_conversations') }}">Conversations supprimées</a>
									</li>
									<li class="nav-item">
										<a class="nav-link text-warning" href="{{ path('admin_subscriptions_index') }}">Gérer Abonnements</a>
									</li>
								{% endif %}
							{% endif %}
						</ul>
					</div>
				</div>
			</nav>
		</header>

		{# CONTENU PRINCIPAL #}
		<main class="container mt-4 flex-grow-1"> {% block body %}{% endblock %}
			</main>

			{# FOOTER #}
			<footer class="bg-dark text-white text-center py-3 mt-auto">
				<p>&copy;
					{{ "now"|date("Y") }}
					Site de Rencontre. Tous droits réservés.</p>
			</footer>

			{# Bootstrap JS #}
			 <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
		</body>
	</html>
