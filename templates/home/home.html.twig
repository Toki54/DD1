{% extends 'base.html.twig' %}

{% block title %}Accueil - Site de Rencontre
{% endblock %}

{% block body %}
	<section class="text-center p-4 text-white">
		<h1 class="display-5 fw-bold">Bienvenue sur Dial And Date</h1>
		<p class="lead mt-3">
			Un site de rencontre conçu pour les personnes qui ne veulent pas d’enfants.<br>
			Parce que le sujet des enfants est fondamental, nous pensons qu’il est sage d’être clair dès le départ.<br>
			Ici, vous trouverez des profils partageant votre vision.
		</p>
	</section>

	{% if not app.user %}
		<div class="row justify-content-center text-dark">
			<div class="col-md-5">
				<div class="card text-center p-4">
					<h2 class="mb-3">Déjà inscrit ?</h2>
					<a href="{{ path('app_login') }}" class="btn btn-outline-dark">Se connecter</a>
				</div>
			</div>
			<div class="col-md-5">
				<div class="card text-center p-4">
					<h2 class="mb-3">Nouvel utilisateur ?</h2>
					<a href="{{ path('app_register') }}" class="btn btn-dark">Créer un compte</a>
				</div>
			</div>
		</div>
	{% else %}
		<div class="text-center mt-5">
			<h2 class="fw-bold">Bienvenue
				{{ app.user.pseudo }}
				👋</h2>
			<p class="mt-3">Explorez les profils, échangez des messages, ou gérez votre abonnement ci-dessous.</p>
			<div class="d-flex justify-content-center gap-3 mt-4 flex-wrap">
				<a class="btn btn-outline-light" href="{{ path('app_profiles_list') }}">🧑‍🤝‍🧑 Voir les profils</a>
				<a class="btn btn-outline-light" href="{{ path('app_messages') }}">📩 Mes messages</a>
				<a class="btn btn-outline-light" href="{{ path('app_stripe_subscribe') }}">💳 Gérer mon abonnement</a>
			</div>
		</div>
	{% endif %}
{% endblock %}

