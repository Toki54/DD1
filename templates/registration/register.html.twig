{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/register.css') }}">
{% endblock %}

{# reCAPTCHA v2 checkbox #}
<script src="https://www.google.com/recaptcha/api.js" async defer></script>

<div class="auth-page">
    <div class="auth-card">

        <header class="auth-header">
            <h1>Créer un compte</h1>
            <p>Inscris-toi pour accéder aux profils et aux messages.</p>
        </header>

        {# erreurs captcha / autres #}
        {% for message in app.flashes('error') %}
            <div class="alert alert-danger">
                {{ message }}
            </div>
        {% endfor %}

        {{ form_start(registrationForm, { 'attr': {'class': 'auth-form'} }) }}

            <div class="form-group">
                {{ form_row(registrationForm.email) }}
            </div>

            <div class="form-group">
                {{ form_row(registrationForm.pseudo) }}
            </div>

            <div class="form-group">
                {{ form_row(registrationForm.plainPassword, { label: 'Mot de passe' }) }}
            </div>

            {# ✅ CAPTCHA #}
            <div class="form-group">
                <div class="g-recaptcha" data-sitekey="{{ recaptcha_site_key }}"></div>
            </div>

            <button type="submit" class="btn btn-primary w-100">
                S'inscrire
            </button>

            {# zone bas : agree + déjà un compte #}
            <div class="auth-bottom">

                <div class="agree-row">
                    {{ form_widget(registrationForm.agreeTerms) }}
                    <label for="{{ registrationForm.agreeTerms.vars.id }}">
                        J’accepte les conditions d’utilisation
                        <a href="#" class="terms-link" data-bs-toggle="modal" data-bs-target="#termsModal">
                            (lire)
                        </a>
                    </label>
                </div>

                <div class="auth-footer">
                    <span>Déjà un compte ?</span>
                    <a href="{{ path('app_login') }}">Se connecter</a>
                </div>

            </div>

        {{ form_end(registrationForm) }}

    </div>
</div>

{# ✅ MODAL CONDITIONS #}
<div class="modal fade" id="termsModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Conditions d’utilisation</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Fermer"></button>
            </div>

            <div class="modal-body">
                <p><strong>En cochant la case, vous acceptez :</strong></p>
                <ul>
                    <li>Être majeur et fournir des informations exactes (email, pseudo, etc.).</li>
                    <li>Ne pas publier de contenu illégal, haineux, harcelant, sexuel non-consenti, ou contraire à la loi.</li>
                    <li>Respecter les autres utilisateurs (pas de spam, pas d’arnaque, pas d’usurpation d’identité).</li>
                    <li>Comprendre que certains contenus/accès peuvent dépendre d’un abonnement (Premium).</li>
                    <li>Accepter que votre compte puisse être suspendu en cas d’abus ou non-respect des règles.</li>
                </ul>

                <p class="mb-0">
                    Tu peux remplacer ce texte par tes vraies CGU (copier/coller) quand tu veux.
                </p>
            </div>

            <div class="modal-footer">
                <button type="button" class="btn btn-outline" data-bs-dismiss="modal">Fermer</button>
            </div>
        </div>
    </div>
</div>

{% endblock %}
